def get_file_list(config_path: str) -> list:
    """
    Reads the config file, parses include/exclude rules,
    and returns a definitive list of absolute file paths to scan.

    Args:
        config_path (str): The path to the JSON configuration file.

    Returns:
        list: A list of absolute file paths (strings) to be scanned.
    """
    # Harshit's implementation will replace this.
    pass
