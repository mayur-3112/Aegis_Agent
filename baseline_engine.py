def create_baseline(file_list: list) -> dict:
    """
    Takes a list of files, hashes each one using BLAKE3,
    and returns a dictionary of {filepath: hash}.

    Args:
        file_list (list): A list of absolute file paths from the Discovery Engine.

    Returns:
        dict: A dictionary of {filepath: hash}.
    """
    # Keshav's implementation will replace this.
    pass
